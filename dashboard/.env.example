# The API. Must be reachable from the browser, not just from the server — the dashboard
# talks to it directly from the client, so this value ends up in the bundle.
# The API must list this dashboard's origin in its CORS_ORIGINS (http://localhost:3000).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
